node_modules/
build/
dist/
coverage/


# Leftover user store from the removed auth system. Kept ignored so an old
# copy with password hashes can never be committed by accident.
# Leading slash matters: a bare "data/" would also ignore src/data/.
/data/
.DS_Store
*.log
.env*
!.env.example

